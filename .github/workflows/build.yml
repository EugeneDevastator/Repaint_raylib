name: Build & Release

on:
  push:
    branches: ["**"]

jobs:
  app-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libx11-dev libxrandr-dev libxinerama-dev \
            libxcursor-dev libxi-dev libgl1-mesa-dev

      - name: Configure
        run: cmake --preset linux-release

      - name: Build
        run: cmake --build build/linux-release --parallel

      - name: Package
        run: |
          mkdir -p dist/repaint-linux
          cp build/linux-release/bin/repaint dist/repaint-linux/
          cp -r shaders dist/repaint-linux/ 2>/dev/null || true
          cp -r resources dist/repaint-linux/ 2>/dev/null || true
          zip -r repaint-linux.zip dist/repaint-linux

      - uses: actions/upload-artifact@v4
        with:
          name: app-linux
          path: repaint-linux.zip

  app-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          install: mingw-w64-x86_64-gcc mingw-w64-x86_64-cmake mingw-w64-x86_64-make zip

      - name: Configure
        shell: msys2 {0}
        run: cmake --preset win64-release -DCMAKE_MAKE_PROGRAM=mingw32-make

      - name: Build
        shell: msys2 {0}
        run: cmake --build build/win64-release --parallel

      - name: Package
        shell: msys2 {0}
        run: |
          mkdir -p dist/repaint-windows
          cp build/win64-release/bin/repaint.exe dist/repaint-windows/
          cp "$MINGW_PREFIX/bin/libwinpthread-1.dll"  dist/repaint-windows/
          cp "$MINGW_PREFIX/bin/libgcc_s_seh-1.dll"   dist/repaint-windows/
          cp "$MINGW_PREFIX/bin/libstdc++-6.dll"       dist/repaint-windows/
          cp -r shaders   dist/repaint-windows/ 2>/dev/null || true
          cp -r resources dist/repaint-windows/ 2>/dev/null || true
          cd dist && zip -r ../repaint-windows.zip repaint-windows

      - uses: actions/upload-artifact@v4
        with:
          name: app-windows
          path: repaint-windows.zip

  app-windows-32:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW32
          install: mingw-w64-i686-gcc mingw-w64-i686-cmake mingw-w64-i686-make mingw-w64-i686-zip

      - name: Configure
        shell: msys2 {0}
        run: cmake --preset win32-release -DBUILD_NNSERVER=OFF -DCMAKE_MAKE_PROGRAM=mingw32-make

      - name: Build
        shell: msys2 {0}
        run: cmake --build build/win32-release --target repaint copy_shaders copy_resources --parallel

      - name: Package
        shell: msys2 {0}
        run: |
          mkdir -p dist/repaint-windows-32
          cp build/win32-release/bin/repaint.exe dist/repaint-windows-32/
          cp "$MINGW_PREFIX/bin/libwinpthread-1.dll"  dist/repaint-windows-32/
          cp "$MINGW_PREFIX/bin/libgcc_s_dw2-1.dll"   dist/repaint-windows-32/
          cp "$MINGW_PREFIX/bin/libstdc++-6.dll"       dist/repaint-windows-32/
          cp -r shaders   dist/repaint-windows-32/ 2>/dev/null || true
          cp -r resources dist/repaint-windows-32/ 2>/dev/null || true
          cd dist && zip -r ../repaint-windows-32.zip repaint-windows-32

      - uses: actions/upload-artifact@v4
        with:
          name: app-windows-32
          path: repaint-windows-32.zip

  app-linux-32:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies (i386)
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y \
            gcc-multilib g++-multilib pkg-config zip \
            libx11-dev:i386 libxrandr-dev:i386 libxinerama-dev:i386 \
            libxcursor-dev:i386 libxi-dev:i386 libxrender-dev:i386 \
            libxfixes-dev:i386 libxext-dev:i386 libxkbcommon-dev:i386 \
            libgl1-mesa-dev:i386

      - name: Configure
        run: cmake --preset linux32-release -DBUILD_NNSERVER=OFF -DCMAKE_LIBRARY_PATH=/usr/lib/i386-linux-gnu

      - name: Build
        run: cmake --build build/linux32-release --target repaint copy_shaders copy_resources --parallel

      - name: Package
        run: |
          mkdir -p dist/repaint-linux-32
          cp build/linux32-release/bin/repaint dist/repaint-linux-32/
          cp -r shaders dist/repaint-linux-32/ 2>/dev/null || true
          cp -r resources dist/repaint-linux-32/ 2>/dev/null || true
          cd dist && zip -r ../repaint-linux-32.zip repaint-linux-32

      - uses: actions/upload-artifact@v4
        with:
          name: app-linux-32
          path: repaint-linux-32.zip

  app-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure
        run: cmake --preset mac-release -DBUILD_NNSERVER=OFF

      - name: Build
        run: cmake --build build/mac-release --target repaint copy_shaders copy_resources --parallel

      - name: Package
        run: |
          mkdir -p dist/repaint-mac
          cp build/mac-release/bin/repaint dist/repaint-mac/
          cp -r shaders dist/repaint-mac/ 2>/dev/null || true
          cp -r resources dist/repaint-mac/ 2>/dev/null || true
          cd dist && zip -r ../repaint-mac-app.zip repaint-mac

      - uses: actions/upload-artifact@v4
        with:
          name: app-mac
          path: repaint-mac-app.zip

  nnserver-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build
        run: |
          set -ex
          pip install numpy Pillow torch transformers
          pip freeze | grep -iv '^pip=\|^setuptools=\|^wheel=\|^-e ' > NNModelServer/requirements.txt

          curl -sL "https://github.com/astral-sh/python-build-standalone/releases/download/20250115/cpython-3.11.11+20250115-x86_64-unknown-linux-gnu-install_only_stripped.tar.gz" -o python.tar.gz
          mkdir -p pyextract
          tar -xzf python.tar.gz -C pyextract
          cd pyextract
          topdir="$(ls -A | head -1)"
          if [ "$topdir" != "python" ]; then mv "$topdir" python; fi
          python/bin/python3 -m ensurepip --upgrade 2>/dev/null || true
          zip -r ../python.zip python
          cd ..
          rm -rf pyextract python.tar.gz

          pip install pyinstaller
          pyinstaller --onefile NNModelServer/nnserver_bootstrap.py --name nnserver \
            --add-data python.zip:. \
            --add-data NNModelServer/requirements.txt:. \
            --exclude-module numpy --exclude-module PIL --exclude-module torch \
            --exclude-module transformers --exclude-module huggingface_hub \
            --exclude-module matplotlib --exclude-module scipy --exclude-module sympy \
            --exclude-module pandas --exclude-module tkinter
          rm -rf python.zip

          mkdir -p dist-server
          cp dist/nnserver dist-server/
          cp NNModelServer/nnserver_core.py dist-server/

      - name: Package
        run: zip -r nnserver-linux.zip dist-server

      - uses: actions/upload-artifact@v4
        with:
          name: nnserver-linux
          path: nnserver-linux.zip

  nnserver-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build
        shell: bash
        run: |
          set -ex
          pip install numpy Pillow torch transformers
          pip freeze | grep -iv '^pip=\|^setuptools=\|^wheel=\|^-e ' > NNModelServer/requirements.txt

          curl -sL "https://github.com/astral-sh/python-build-standalone/releases/download/20250115/cpython-3.11.11+20250115-x86_64-pc-windows-msvc-shared-install_only_stripped.tar.gz" -o python.tar.gz
          mkdir -p pyextract
          tar -xzf python.tar.gz -C pyextract
          cd pyextract
          topdir="$(ls -A | head -1)"
          if [ "$topdir" != "python" ]; then mv "$topdir" python; fi
          python/python.exe -m ensurepip --upgrade 2>/dev/null || true
          python -c "import shutil; shutil.make_archive('../python', 'zip', 'python')"
          cd ..
          rm -rf pyextract python.tar.gz

          pip install pyinstaller
          pyinstaller --onefile NNModelServer/nnserver_bootstrap.py --name nnserver \
            --add-data python.zip:. \
            --add-data NNModelServer/requirements.txt:. \
            --exclude-module numpy --exclude-module PIL --exclude-module torch \
            --exclude-module transformers --exclude-module huggingface_hub \
            --exclude-module matplotlib --exclude-module scipy --exclude-module sympy \
            --exclude-module pandas --exclude-module tkinter
          rm -rf python.zip

          mkdir -p dist-server
          cp dist/nnserver.exe dist-server/
          cp NNModelServer/nnserver_core.py dist-server/

      - name: Package
        shell: powershell
        run: |
          Compress-Archive -Path dist-server -DestinationPath nnserver-windows.zip

      - uses: actions/upload-artifact@v4
        with:
          name: nnserver-windows
          path: nnserver-windows.zip

  nnserver-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build
        run: |
          set -ex
          pip install numpy Pillow torch transformers
          pip freeze | grep -iv '^pip=\|^setuptools=\|^wheel=\|^-e ' > NNModelServer/requirements.txt

          ARCH=$(uname -m)
          if [ "$ARCH" = "arm64" ]; then
            TRIPLE="aarch64-apple-darwin"
          else
            TRIPLE="x86_64-apple-darwin"
          fi

          curl -sL "https://github.com/astral-sh/python-build-standalone/releases/download/20250115/cpython-3.11.11+20250115-${TRIPLE}-install_only_stripped.tar.gz" -o python.tar.gz
          mkdir -p pyextract
          tar -xzf python.tar.gz -C pyextract
          cd pyextract
          topdir="$(ls -A | head -1)"
          if [ "$topdir" != "python" ]; then mv "$topdir" python; fi
          python/bin/python3 -m ensurepip --upgrade 2>/dev/null || true
          zip -r ../python.zip python
          cd ..
          rm -rf pyextract python.tar.gz

          pip install pyinstaller
          pyinstaller --onefile NNModelServer/nnserver_bootstrap.py --name nnserver \
            --add-data python.zip:. \
            --add-data NNModelServer/requirements.txt:. \
            --exclude-module numpy --exclude-module PIL --exclude-module torch \
            --exclude-module transformers --exclude-module huggingface_hub \
            --exclude-module matplotlib --exclude-module scipy --exclude-module sympy \
            --exclude-module pandas --exclude-module tkinter
          rm -rf python.zip

          mkdir -p dist-server
          cp dist/nnserver dist-server/
          cp NNModelServer/nnserver_core.py dist-server/

      - name: Package
        run: zip -r nnserver-mac.zip dist-server

      - uses: actions/upload-artifact@v4
        with:
          name: nnserver-mac
          path: nnserver-mac.zip

  bundle:
    needs:
      - app-linux
      - app-windows
      - app-linux-32
      - app-windows-32
      - app-mac
      - nnserver-linux
      - nnserver-windows
      - nnserver-mac
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: _dl
          merge-multiple: true

      - name: Bundle linux
        run: |
          mkdir -p _out/repaint-linux _tmp/al
          unzip -o _dl/repaint-linux.zip -d _tmp/al || true
          cp -r _tmp/al/dist/repaint-linux/* _out/repaint-linux/
          mkdir -p _tmp/lnn
          unzip -o _dl/nnserver-linux.zip -d _tmp/lnn || true
          mkdir -p _out/repaint-linux/NNServer
          cp -r _tmp/lnn/dist-server/* _out/repaint-linux/NNServer/
          cd _out && zip -r ../repaint-linux-full.zip repaint-linux

      - name: Bundle windows
        run: |
          mkdir -p _out/repaint-windows _tmp/aw
          unzip -o _dl/repaint-windows.zip -d _tmp/aw || true
          cp -r _tmp/aw/repaint-windows/* _out/repaint-windows/
          mkdir -p _tmp/wnn
          unzip -o _dl/nnserver-windows.zip -d _tmp/wnn || true
          mkdir -p _out/repaint-windows/NNServer
          cp -r _tmp/wnn/dist-server/* _out/repaint-windows/NNServer/
          cd _out && zip -r ../repaint-windows-full.zip repaint-windows

      - name: Bundle mac
        run: |
          mkdir -p _out/repaint-mac _tmp/mnn _tmp/ma
          unzip -o _dl/repaint-mac-app.zip -d _tmp/ma || true
          cp -r _tmp/ma/repaint-mac/* _out/repaint-mac/
          unzip -o _dl/nnserver-mac.zip -d _tmp/mnn || true
          mkdir -p _out/repaint-mac/NNServer
          cp -r _tmp/mnn/dist-server/* _out/repaint-mac/NNServer/
          cd _out && zip -r ../repaint-mac-full.zip repaint-mac

      - name: Bundle linux-32
        run: |
          cp _dl/repaint-linux-32.zip ./repaint-linux-32.zip

      - name: Bundle windows-32
        run: |
          cp _dl/repaint-windows-32.zip ./repaint-windows-32.zip

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest Build
          prerelease: true
          files: |
            repaint-linux-full.zip
            repaint-windows-full.zip
            repaint-mac-full.zip
            repaint-linux-32.zip
            repaint-windows-32.zip
